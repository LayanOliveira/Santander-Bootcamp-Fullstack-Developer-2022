# Collections

## Atividade 1: Maps

Create a function that returns the name of members of a Map with the 'ADMIN' role on the system.

1. Create a function `getAdmins` which receives a `Map`;
2. Create a Map and populate it with user's names and their roles on the system. (Ex: 'Luiz' => 'Admin');
3. Inside `getAdmins`, utilize a loop `for...of` to return a list with names of users with the Admin role.

## Atividade 2: Sets

Given the array `[30, 30, 40, 5, 223, 2049, 3034, 5]`, return another array with unique values.

# Créditos 

[Stebsnusch](https://github.com/LayanOliveira/basecamp-javascript/tree/main/colecoes)